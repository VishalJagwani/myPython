# Tuple : Immutable lists
my_tuple = (1, 2, 3, 4, 5, 6, 5)
# my_tuple[0] = 'z' cannot be done

# user = {
#     (1, 2): [1, 2, 3],
#     'greet': 'Hello',
#     'age': 20
# }
# print(user[(1, 2)])

# x, y, z, *other = (1, 2, 3, 4, 5, 6)
# print(other)

print(my_tuple.count(5))
print(my_tuple.index(2))
print(len(my_tuple))
